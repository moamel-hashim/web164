const firstNumber = Number(window.prompt('enter a number'));
const secondNumber = Number(window.prompt('enter a second number'))
window.alert(`the total for the numbers you've entered is ${firstNumber + secondNumber}`)
